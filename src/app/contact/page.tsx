import ContactPage from "@/components/ContactPage";

export const metadata = {
  title: "Contact — Reside",
  description: "Reach out to the Reside team for booking support, property questions, or personalized guidance.",
};

export default function ContactRoute() {
  return <ContactPage />;
}
